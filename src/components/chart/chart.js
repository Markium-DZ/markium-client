import { memo, forwardRef } from 'react';

// Stub: ApexCharts was removed — active dashboard uses @mui/x-charts instead.
// Template sections that import this component are not used in production routes.
const Chart = forwardRef((props, ref) => null);

export default memo(Chart);
